import React from 'react';
import { Input } from 'antd';
const { TextArea } = Input;

const Step3 = (props) => {
  return (
    <TextArea rows={20} />
  );
};

Step3.propTypes = {};
Step3.defaultProps = {};

export default Step3;
