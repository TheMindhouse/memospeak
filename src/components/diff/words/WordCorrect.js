import React from 'react'
import PropTypes from 'prop-types'

const WordCorrect = ({ value }) => {
  return (
    <span>{value}</span>
  )
}

WordCorrect.propTypes = {}
WordCorrect.defaultProps = {}

export default WordCorrect
